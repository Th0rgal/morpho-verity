{
  function oraclePrice(oracle) -> price {
    // Stub: calls the oracle contract to get the collateral price.
    // In production, this would be a staticcall to the oracle address.
    price := 0
  }
}
