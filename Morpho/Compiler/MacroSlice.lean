import Morpho.Contract

namespace Morpho.Compiler.MacroSlice

/-- Compatibility spec alias only. The canonical deployed source is `Morpho.Contract.Morpho`. -/
def spec := _root_.Morpho.Contract.Morpho.spec

end Morpho.Compiler.MacroSlice
